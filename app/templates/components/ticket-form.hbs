<div class="row">
  <div class="col-md-12">
    <div class="block-flat">
      <div class="header">              
        <h3>Ticket Form</h3>
      </div>
      <div class="content">
         <form class="form-horizontal group-border-dashed" {{action "createTicket" on="submit"}} style="border-radius: 0px;">
          <div class="form-group">
            <label class="col-sm-3 control-label">Subject</label>
            <div class="col-sm-6">
              {{input value=subject type="text" class="form-control"}}
            </div>
          </div>

          <div class="form-group">
            <label class="col-sm-3 control-label">Description</label>
            <div class="col-sm-6">
              {{textarea value=content type="text" class="form-control"}}
            </div>
            {{#if errorMessage}}
              {{errorMessage}}
            {{/if}}
          </div>
          <div class="form-group">
            <div class="col-sm-offset-3 col-sm-6">
              <button type="submit" class="btn btn-primary btn-block">Send</button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
